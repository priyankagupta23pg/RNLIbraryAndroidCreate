/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Signup from './source/screens/signup';
import {name as appName} from './appp.json';

AppRegistry.registerComponent(appName, () => Signup);
